gcs {
  user-created-timeline {
    projectId = ${GCP_PROJECT_ID}
    bucketName = ${GCP_PROJECT_ID}"_analytics"
  }
  sendgrid-contacts-sync {
    projectId = ${GCP_PROJECT_ID}
    bucketName = ${GCP_PROJECT_ID}"_sendgrid-contacts-sync"
  }
  firebase {
    projectId = ${GCP_PROJECT_ID}
    bucketName = ${GCP_PROJECT_ID}".appspot.com"
  }
}